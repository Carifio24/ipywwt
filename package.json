{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/widget.js --minify --format=esm --bundle --outdir=src/ipywwt/static"
	},
	"dependencies": {
		"@wwtelescope/engine": "^7.30.1"
	},
	"devDependencies": {
		"esbuild": "0.24.2"
	}
}
